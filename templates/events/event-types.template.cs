namespace ${NAMESPACE}.${MODULE_NAME}.Events
{
    /// <summary>
    /// Event type constants for ${ENTITY_NAME}.
    /// </summary>
    public static class ${ENTITY_NAME}EtoTypes
    {
        /// <summary>
        /// Event type for ${ENTITY_NAME} creation.
        /// </summary>
        public const string Created = "${NAMESPACE}.${MODULE_NAME}.${ENTITY_NAME}.Created";
        
        /// <summary>
        /// Event type for ${ENTITY_NAME} update.
        /// </summary>
        public const string Updated = "${NAMESPACE}.${MODULE_NAME}.${ENTITY_NAME}.Updated";
        
        /// <summary>
        /// Event type for ${ENTITY_NAME} deletion.
        /// </summary>
        public const string Deleted = "${NAMESPACE}.${MODULE_NAME}.${ENTITY_NAME}.Deleted";
    }
}

